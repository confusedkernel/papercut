{{- $pctx := . -}}
{{- if .IsHome -}}{{ $pctx = .Site }}{{- end -}}
{{- $pages := slice -}}
{{- if or $.IsHome $.IsSection -}}
  {{- $pages = $pctx.RegularPages -}}
{{- else -}}
  {{- $pages = $pctx.Pages -}}
{{- end -}}
{{- $limit := .Site.Config.Services.RSS.Limit -}}
{{- if ge $limit 1 -}}
  {{- $pages = $pages | first $limit -}}
{{- end -}}
{{- $author := site.Params.author | default site.Title -}}
{{- printf "<?xml version=\"1.0\" encoding=\"utf-8\" standalone=\"yes\"?>" | safeHTML }}
<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:dc="http://purl.org/dc/elements/1.1/" xmlns:content="http://purl.org/rss/1.0/modules/content/">
  <channel>
    <title>{{ if eq .Title site.Title }}{{ site.Title }}{{ else }}{{ with .Title }}{{ . }} on {{ end }}{{ site.Title }}{{ end }}</title>
    <link>{{ .Permalink }}</link>
    <description>{{ with site.Params.description }}{{ . }}{{ else }}Recent content{{ with .Title }} in {{ . }}{{ end }} on {{ site.Title }}{{ end }}</description>
    <generator>Hugo</generator>
    {{- with site.Language.LanguageCode }}
    <language>{{ . }}</language>
    {{- end }}
    {{- with site.Params.copyright }}
    <copyright>{{ . }}</copyright>
    {{- end }}
    {{- if not .Date.IsZero }}
    <lastBuildDate>{{ .Date.Format "Mon, 02 Jan 2006 15:04:05 -0700" | safeHTML }}</lastBuildDate>
    {{- end }}
    {{- with .OutputFormats.Get "RSS" }}
    {{ printf "<atom:link href=%q rel=\"self\" type=%q />" .Permalink .MediaType | safeHTML }}
    {{- end }}
    {{- range $pages }}
    {{- $base := .Permalink -}}
    {{/* Rewrite page-relative src/href values to absolute URLs. The image shortcode
         emits page-relative paths (e.g. src="src/image.png") which resolve correctly in
         a browser but break in a feed reader. Go's RE2 has no lookahead, so this matches
         on "starts with an alphanumeric and contains no colon", which excludes absolute
         paths (/…), fragments (#…), and anything with a scheme (https:, data:, mailto:). */}}
    {{- $content := replaceRE `(src|href)="([a-zA-Z0-9_.][^":]*)"` (printf `${1}="%s${2}"` $base) .Content -}}
    <item>
      <title>{{ .Title }}</title>
      <link>{{ .Permalink }}</link>
      {{- if not .Date.IsZero }}
      <pubDate>{{ .Date.Format "Mon, 02 Jan 2006 15:04:05 -0700" | safeHTML }}</pubDate>
      {{- end }}
      <dc:creator>{{ $author }}</dc:creator>
      <guid isPermaLink="true">{{ .Permalink }}</guid>
      {{- with .Description }}
      <description>{{ . }}</description>
      {{- else }}
      <description>{{ .Summary | plainify | truncate 300 }}</description>
      {{- end }}
      <content:encoded>{{ $content | transform.XMLEscape | safeHTML }}</content:encoded>
    </item>
    {{- end }}
  </channel>
</rss>
